query Query($input: UserLoginInput!) {
  login(input: $input) {
    token
    user {
      id
      email
      firstName
      lastName
      status
      story
      createdAt
      updatedAt
      role
      location
      avatarUrl
    }
  }
}