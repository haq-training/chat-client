mutation Register($input: CreateUserInput!) {
    register(input: $input) {
        id
        email
        firstName
        lastName
        role
        status
        avatarUrl
        location
        story
        createdAt
        updatedAt
    }
}